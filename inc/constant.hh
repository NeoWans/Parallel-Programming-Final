#pragma once
#include <cstddef>
const size_t        cases         = 11;
const size_t        matrix_max_sz = 85401;
extern const size_t matrix_sz[cases + 2];
#define lengthof(__array) (sizeof(__array) / sizeof((__array)[0]))